numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
PyYAML>=6.0
matplotlib>=3.7
pytest>=7.0
# Optional boosted-tree engines. The framework falls back to sklearn GradientBoosting if unavailable.
xgboost>=1.7; python_version < '3.14'
lightgbm>=4.0; python_version < '3.14'

# Optional real-data downloader dependency.
ccxt>=4.0

# Optional WRDS inventory connector. The code imports this lazily and does not
# require credentials or the package for tests/report planning.
wrds>=3.2

# Optional Parquet writer for feature-panel artifacts.
pyarrow>=15.0
